@model IEnumerable<ExpenseVoyage.Models.Expenses>

@{
    Layout = "~/Views/Shared/User_Layout.cshtml";
}

<div class="breadcumb-wrapper" data-bg-src="@Url.Content("~/asset/img/normal/breadcumb-bg.jpg")">
    <div class="container">
        <div class="breadcumb-content">
            <h1 class="breadcumb-title">Trip Booking</h1>
            <ul class="breadcumb-menu">
                <li><a asp-action="Index">Home</a></li>
                <li>Contact Us</li>
            </ul>
        </div>
    </div>
</div>

<div class="space-extra2-top space-extra2-bottom">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h2>Your Trips</h2>
                <table class="table">
                    <thead>
                        <tr>
                            <th>Trip Name</th>
                            <th>Category</th>
                            <th>Amount</th>
                            <th>Date</th>
                            <th>Notes</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Any())
                        {
                            foreach (var trip in Model)
                            {
                                <tr>
                                    <td>@trip.trip.trip_name</td>
                                    <td>@trip.category.CategoryName</td>
                                    <td>@trip.Amount</td>
                                    <td>@trip.expanse_Date.ToString("d")</td>
                                    <td>@trip.notes</td>
                                    <td>
                                        <a asp-action="Edit_Expense" asp-route-id="@trip.Id" class="btn btn-primary">Edit</a>
                                        <a asp-action="Delete_Expense" asp-route-id="@trip.Id" class="btn btn-danger">Delete</a>
                                    </td>
                                </tr>

                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="7">No trips found.</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
